

## Что делает проект
- `GET /` отдает HTML-страницу с кнопкой `сделать запрос на АПИ`.
- При клике кнопка отправляет `GET /api`.
- Сервер отвечает JSON: `{ "message": "Запрос прошел успешно" }`.
- Клиент выводит это сообщение в консоль браузера.

## Запуск
   - `npm install`
   - `npm start`
   - `http://localhost:3000`.
