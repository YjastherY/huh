{
  "name": "fastify-api-demo",
  "version": "1.0.0",
  "description": "Simple Fastify server with / and /api endpoints",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  }
}
